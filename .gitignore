# Dependencies
node_modules/

# Capacitor / Android build output
www/
android/

# OS / editor files
.DS_Store
Thumbs.db
*.log

# PHP / env
.phpintel/
.vscode/
.idea/
*.cache

# Keep uploads folder but ignore user-uploaded binaries if desired
# (uploads/ is kept so the app has its assets)
