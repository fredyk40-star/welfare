# Dependencies
node_modules/

# Capacitor / Android build output
www/
android/

# OS / editor files
.DS_Store
Thumbs.db
*.log

# Kilo agent working directory (local only)
.kilo/

# PHP / env
# NOTE: config/database.php is intentionally COMMITTED — it contains no secrets,
# only the env-var loader + Database class. Vercel needs it to run.
.env
.env.local
.env.*.local
.phpintel/
.vscode/
.idea/
*.cache

# Keep uploads folder but ignore user-uploaded binaries if desired
# (uploads/ is kept so the app has its assets)
uploads/*.jpg
uploads/*.jpeg
uploads/*.png
uploads/*.gif
uploads/*.pdf
uploads/*.crdownload

# Local audit reports and temp files
New Folder/

.vercel
