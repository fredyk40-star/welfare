# Vercel build exclusions - keep the serverless function bundle small
node_modules/
android/
www/
.kilo/
.vscode/
uploads/
*.log

# Local-only / secret files
.env
.env.local
# Note: service-worker.js is intentionally NOT ignored — it must be deployed for the PWA.

